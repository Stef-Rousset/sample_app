<li>
  <% if current_user.admin? && !current_user?(user) %>
  <%= gravatar_for user, size: 50 %> <!-- gravatar_for =>helper ds users_helper.rb -->
  <%= link_to user.name, user %>
  <%=  link_to "delete", user, method: :delete, data: { confirm: "Are you sure ?"} %>
  <% end %>
</li>
